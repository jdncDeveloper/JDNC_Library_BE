spring:
  profiles:
    active:
      - production

    group:
      local:
        - localdb
        - secret
        - smtp
        - springdoc
        - naverapi

      production:
        - production
        - secret
        - smtp
        - springdoc
        - naverapi
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true

lms:
  login-url: https://lms.jdnc.or.kr/auth/login_check
  my-info-url: https://lms.jdnc.or.kr/mypage/myInfo

init:
  admin:
    username: admin
    password: test1234

management:
  endpoints:
    web:
      base-path: /
      path-mapping:
        health: /health-check

  endpoint:
    health:
      show-details: always